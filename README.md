Ejemplos para la exposición de XML - Animaciones en Flutter

Para el ejemplo del juego usar en el puspec.yaml en dependencies: flame: ^1.18.0

Para el ejemplo del juego usar en el puspec.yaml en dependencies: lottie: ^3.1.0
